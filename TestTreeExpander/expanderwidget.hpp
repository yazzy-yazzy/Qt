#ifndef EXPANDERWIDGET_HPP
#define EXPANDERWIDGET_HPP

#include <QTreeWidget>

class ExpanderWidget : public QTreeWidget
{
    Q_OBJECT
public:
    explicit ExpanderWidget(QWidget *parent = nullptr);

signals:

public slots:
};

#endif // EXPANDERWIDGET_HPP
